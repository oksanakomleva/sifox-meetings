# E2E test credentials — copy to .env.test and fill in real values
# .env.test is gitignored and never committed

# Must match TEST_API_KEY variable in Railway
TEST_API_KEY=your-secret-test-key-here

# Railway deployment URL (default is already set in smoke.py)
# BASE_URL=https://sifox-meetings.up.railway.app
